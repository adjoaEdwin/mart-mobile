import colors from "./colors";

const fonts = {
  title: "Arial",
  text: "SanFrancisco",
  code: "Fira",
  heading: {
    color: colors.title,
    fontSize: 20,
    textAlign: "center"
  },
  text: {
    color: colors.text,
    fontSize: 17,
    textAlign: "center"
  }
};

export default fonts;
